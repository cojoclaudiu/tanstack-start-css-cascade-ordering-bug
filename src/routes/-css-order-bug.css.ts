import { style } from '@vanilla-extract/css'

export const page = style({
  display: 'grid',
  gap: '16px',
})

export const consumerOverride = style({
  background: '#dcfce7',
  color: '#166534',
})
